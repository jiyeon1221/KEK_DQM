# Python 캐시
__pycache__/
*.pyc
*.pyo

# macOS
.DS_Store

# 가상환경
ai/

# 에디터
.vscode/
.cursor/
.claude/

# 로그
logs/

# finetuning — 소스 스크립트(.py/.sh)는 포함, 대용량 생성물만 제외
finetuning/data/
finetuning/logs/

# logging 폴더 — 민감 JSON만 제외, 폴더는 .gitkeep으로 유지
logging/*.json

# DAQ 전체 제외
DAQ/

# DAQ_JBNU 전체 제외 (임베디드 git 저장소)
DAQ_JBNU/

# DQM 빌드 / 컴파일 아티팩트
DQM/output/
DQM/build/
DQM/lib/
DQM/install/

# CERN TB standalone 실행 파일 (소스 .cc만 추적)
DQM/draw_astropix_CERN
DQM/draw_hodoscope_CERN_round
DQM/draw_hodoscope_CERN_square
DQM/normalize_hodo_CERN
DQM/draw_intADC

# 표준 standalone 드로잉 결과물
DQM/AstroPix/
DQM/Avg/
DQM/Hodoscope/

# 참고용 외부 코드 스냅샷 (중첩 git 저장소)
ref_for_cursor/

# AstroPix 토이 데이터
toy_astroPix/

# 토이 데이터셋
toydataset/

# models — 에이전트 폴더 구조만 유지, 내용물 전체 제외
models/*/**
!models/*/.gitkeep

# position scan 결과 — 사용 안 함
position_scan_results/

# train 폴더 전체 제외
train/
